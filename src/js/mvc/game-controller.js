class GameController {
  constructor(model, view) {
    this.model = model;
    this.view = view;
  }

  gameLoop() {
    // update();
    // render();
    // requestAnimationFrame(gameLoop);
  }
}

export { GameController };
